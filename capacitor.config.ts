import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.souqaliraq.app',
  appName: 'سوق العراق',
  webDir: 'dist',
  android: {
    backgroundColor: '#ffffff',
  },
};

export default config;
